Release v0.1.4 (patch)

- Bumped main app version to 0.1.4.
- Fixed TypeScript build errors in renderer (strict unused-var and typing fixes).
- Rebuilt renderer and bundled assets.
- Packaged macOS installers (DMG and ZIP) for Apple Silicon (arm64).

Installer artifacts:
- ScholarOS-0.1.4.dmg
- ScholarOS-darwin-arm64-0.1.4.zip

If you need additional platform builds or notarization, tell me and I will run `npm run make` for those targets.
